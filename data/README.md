# Data Directory

This directory holds simulation output data when saved. It is gitignored by default
(CSV, Parquet, and Pickle files are excluded).

To save simulation results, use the export functionality in the Gradio app or call
`simulation.save_results()` from Python.
